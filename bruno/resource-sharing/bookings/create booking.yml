info:
  name: create booking
  type: http
  seq: 1

http:
  method: PATCH
  url: http://localhost:3000/api/bookings/15
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      { 
        "action": "reject"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
