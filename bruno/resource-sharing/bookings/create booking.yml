info:
  name: create booking
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:3000/api/auth/me
  body:
    type: json
    data: |-
      /*
      {
        "email": "alice@gmail.com",
        "password": "password234"
      }
      */
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
